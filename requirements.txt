# Step 3 needs nothing but this.
pymavlink>=2.4.42

# Step 4 (depth -> obstacle ring). The maths and its tests run on numpy
# alone, so the whole pipeline is developable before the camera arrives.
numpy>=1.24

# Added later, at Step 5, when the camera arrives:
# depthai>=2.24.0
